{{!< default}}
<div class="container index-banner">
            <br><br><br><br>

            <div class="row center">
            {{#if @blog.logo}} <img class="logo-main responsive-img" src="{{@blog.logo}}" alt="ausparanormal"> {{/if}} 
                
            </div>
            <div class="row center">
                <h4 class="header col s12 text-kaz-white">{{@blog.title}}</h4>

                <p class="header col s12 text-kaz-white">{{@blog.description}}</p>
            </div>
            <br><br>

            <br><br>
        </div>
    </div>
    <div class="section grey darken-4">
        <div class="container" id="index-info">
        {{! This is the post loop - each post will be output using this markup }}
{{#get "posts" include="tags,author" limit="all" filter="featured:true"}}
    {{#foreach posts}}
        <div class="row">
            <div class="col s12 m12">
                <!--<div class="card-panel grey">
                <div class="card-content white-text">
                <span class="card-title">{{title}}</span>
                    <p>
                    Posted: {{date published_at format="DD MMMM, YYYY HH:MM:SS"}} <br />
                    Author: {{author.name}} <br />
                        {{excerpt}}
                    </p>
                    <div class="card-action">
                    <a href="{{url absolute="true"}}">Read more</a>
            
                    </div>
                    </div>
                </div>-->

                <div class="card grey">
                {{#if image}}
                    <div class="card-image">
                        <img src="{{image}}">
                        <span class="card-title">{{title}}</span>
                    </div>
                    <div class="card-content black-text ">

                {{else}}
                    <div class="card-content black-text">
                    <span class="card-title white-text">{{title}} <br /></span>
                {{/if}}
                        <div class="chip grey lighten-1">
                            {{#if author.image}}
                            <img src="{{author.image}}" alt="Contact Person">
                            {{/if}}
                            {{author.name}}
                        </div>
                         <div class="chip grey lighten-1 ">
                         <i class="material-icons remove-things">schedule</i>
                            {{date published_at format="DD MMMM, YYYY HH:MM:SS"}}</div>
                            <p>
                            <hr>
                            {{excerpt}}</p>
                    </div>
                    <div class="card-action">
                      <a href="{{url absolute="true"}}" class="red-text">Read more</a>
                    </div>
                </div>
            </div>
        </div>        
    {{/foreach}}
{{/get}}

            
        </div>